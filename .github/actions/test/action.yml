name: 'test'
description: 'Test the code'
inputs:
    tm-npm-token:  # id of input
    description: 'Token for NPM regisrty'
    required: true
runs:
    using: 'docker'
    image: 'Dockerfile'
    env:
    - DATABASE_USER=root
    - DATABASE_PASSWORD=root
    - DATABASE_NAME=test
    - DATABASE_PORT=5432
    - DATABASE_HOST=test-db
    entrypoint: "npm run test"